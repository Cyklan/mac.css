{
  "name": "mac.css",
  "version": "0.0.1",
  "description": "Bringing Mac OS X to the web",
  "main": "dist/index.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass index.scss dist/index.css --style compressed",
    "watch": "sass index.scss dist/index.css -w --style compressed"
  },
  "keywords": [
    "css",
    "macos",
    "macosx"
  ],
  "author": "Cyklan",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.56.0"
  }
}
